// AI-Box: Route all Gradle plugin downloads through Nexus mirror
settingsEvaluated { settings ->
    settings.pluginManagement {
        repositories {
            maven {
                url 'https://nexus.internal/repository/gradle-plugins/'
            }
        }
    }
}
allprojects {
    repositories {
        maven {
            url 'https://nexus.internal/repository/maven-central/'
        }
    }
}
